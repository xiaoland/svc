# Sustainable Vibe Coding distribution

This member builds the `sustainable-vibe-coding` Python distribution: the `svc`
command, its runtime, and the read-only packaged SVC Corpus projection.

The canonical framework source and repository development instructions remain in
the workspace root. This file owns only package-index metadata for the independently
rebuildable member distribution.
