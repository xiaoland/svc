openapi: 3.1.0
info:
  title: Recursive contract fixture
  version: 1.0.0
paths:
  /v1/nodes:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: recursive.schemas.yaml#/$defs/Node
      responses:
        '204':
          description: Accepted
