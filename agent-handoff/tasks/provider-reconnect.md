# Provider Reconnect

- **Objective**: Supervise and reconnect the local Codex app-server while preserving one immutable Issue-to-thread binding, one active-turn boundary, and pending GitHub event refs.
- **Guardrails**: Work only on `feat/issue-20-provider-reconnect` in its dedicated worktree. Never create a replacement provider thread for an existing binding, reconstruct or persist provider transcript, start a parallel turn, guess-retry an ambiguous `turn/start` or `turn/steer`, or modify the bootstrap or primary worktree.
- **Verification**: The full `agent-handoff` test suite passes; focused tests prove same-thread resume, authoritative terminal replay, capped reconnect backoff, durable operator-required handling for ambiguous mutations, continued pending/urgent scheduling, self-origin normalization, and graceful shutdown. Real GitHub/Quick Tunnel/Wrapper/app-server black-box evidence remains required before the Draft PR becomes ready.
- **Current Truth**: Issue #20 confirms continuous low-frequency reconnect with a 30-second cap. Mutating-request ambiguity may recover automatically only after real protocol evidence; without that evidence it remains operator-required and cannot be redelivered. The reviewed implementation now watches idle process exit, resumes only the immutable opaque address, keeps crash loops on a 1/2/4/8/16/30-second cadence, requires authoritative completed/failed/interrupted terminal evidence, and durably fences ambiguous start/steer delivery. The independent project passes 98 tests, the repository passes 158 tests, and changed Python files pass targeted Ruff checks; coverage includes a 128 KiB JSONL item above asyncio's default line limit.
- **Next Step**: Review the final diff and worktree boundary, then publish the Human-authorized Draft PR. Keep the packet until Issue #20 closes; real GitHub/Quick Tunnel/Wrapper/app-server black-box acceptance remains outstanding.
